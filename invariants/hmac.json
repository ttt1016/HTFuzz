{
 "module": "hmac",
 "invariants": [
  {
   "name": "reg2hw.wipe_secret.q_wipe_clears",
   "signal": "reg2hw.wipe_secret.q",
   "rule": "wipe_clears",
   "trigger_regs": [
    {
     "reg": "wipe_secret",
     "data": "0x1"
    }
   ],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.u_hmac_core.secret_key_read_only_leak",
   "signal": "u_dut.u_hmac_core.secret_key",
   "rule": "read_only_leak",
   "trigger_regs": [],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.key_0_wipe_clears",
   "signal": "u_dut.key_0",
   "rule": "wipe_clears",
   "trigger_regs": [
    {
     "reg": "wipe_secret",
     "data": "0x1"
    }
   ],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.u_hmac_core.secret_key_i_read_only_leak",
   "signal": "u_dut.u_hmac_core.secret_key_i",
   "rule": "read_only_leak",
   "trigger_regs": [],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.u_hmac_core.hmac_state_bus_intg_check",
   "signal": "u_dut.u_hmac_core.hmac_state",
   "rule": "bus_intg_check",
   "trigger_regs": [],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.secret_key_wipe_clears",
   "signal": "u_dut.secret_key",
   "rule": "wipe_clears",
   "trigger_regs": [
    {
     "reg": "wipe_secret",
     "data": "0x1"
    }
   ],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.u_msg_fifo.fifo_data_wipe_clears",
   "signal": "u_dut.u_msg_fifo.fifo_data",
   "rule": "wipe_clears",
   "trigger_regs": [
    {
     "reg": "wipe_secret",
     "data": "0x1"
    }
   ],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.u_hmac_core.u_sha2.digest_reg_core_consistent",
   "signal": "u_dut.u_hmac_core.u_sha2.digest",
   "rule": "reg_core_consistent",
   "trigger_regs": [],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.hw2reg.err_code.sw_read_digest_without_idle.d_access_control",
   "signal": "u_dut.hw2reg.err_code.sw_read_digest_without_idle.d",
   "rule": "access_control",
   "trigger_regs": [],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.hw2reg.intr_state.hmac_done.de_interrupt_first_event",
   "signal": "u_dut.hw2reg.intr_state.hmac_done.de",
   "rule": "interrupt_first_event",
   "trigger_regs": [],
   "rationale": "LLM 提取（文本模式）"
  },
  {
   "name": "u_dut.digest_sw_err_code_coherent",
   "signal": "u_dut.digest_sw",
   "rule": "err_code_coherent",
   "trigger_regs": [],
   "rationale": "LLM 提取（文本模式）"
  }
 ],
 "raw": "Let me analyze the OpenTitan HMAC module and extract runtime-checkable security invariants.\n\nThe HMAC module in OpenTitan has these key security mechanisms:\n1. **Wipe secret** (WIPE_SECRET register) - wipes the secret key from the HMAC core\n2. **Secret key storage** (KEY_0 through KEY_31 registers, 1024-bit key)\n3. **CFG register** with hmac_en, sha_en, endian_swap, digest_swap\n4. **CFG_BLOCK (cfg_block)** - blocks writes to config/key registers while operation in progress\n5. **Digest registers** (DIGEST_0 through DIGEST_15)\n6. **STATUS register** (idle, fifo_full, fifo_empty, hmac_running, digest_valid)\n7. **Interrupts** (hmac_done, fifo_empty, hmac_err)\n8. **ERR_CODE register** - error codes (sw_invalid_key_length, sw_read_digest_without_idle, sw_read_message_without_idle, sw_sha_disabled)\n9. **MSG_FIFO** - message FIFO\n10. **TL-UL bus integrity** - intg errors\n11. **Wipe on reset** - key wiped after reset\n\nFrom the provided code snippets:\n- `wipe_secret_we`, `wipe_secret_wd` - the WIPE_SECRET register write\n- `reg2hw.wipe_secret.q` - the value goes to core\n- `secret_key_i[1023:0]` - the secret key input to hmac_core\n- i_pad/o_pad computation from secret_key\n\nKey security invariants for HMAC:\n\n1. **wipe_clears**: Writing WIPE_SECRET must clear the secret key in the core (u_hmac_core.secret_key or the key registers). In OpenTitan HMAC, writing WIPE_SECRET clears the key registers (key_0..key_31 in hmac_core) and also wipes the message FIFO... Actually let me recall: In OpenTitan HMAC, WIPE_SECRET write clears the secret key in the core. The key registers KEY_0..KEY_31 are write-only (WOC - write-only, clear on read? no). Actually KEY registers are `wo` (write-only). Reading them returns 0.\n\nLet me recall the actual OpenTitan HMAC design:\n- KEY_0 to KEY_31: 32 registers of 32 bits = 1024-bit key. These are `wo` registers (write-only). Reading returns 0.\n- WIPE_SECRET: any write wipes the secret key (sets key to 0) in the core.\n- CFG: hmac_en, sha_en, endian_swap, digest_swap. Writes blocked when CFG_BLOCK... Actually there's a \"CFG_BLOCK\" via WEN (regwen) - WCFG (CFG_REGWEN). When HMAC is running (not idle), writes to CFG and KEY are blocked.\n- KEY_REGWEN (KEY_REGWEN register): when key write is disabled, writes to KEY_0..31 are blocked.\n- Digest: DIGEST_0..DIGEST_15, readable only when digest_valid and idle; reading digest while not idle triggers error (sw_read_digest_without_idle).\n- ERR_CODE: error codes.\n- STATUS: idle, fifo_full, fifo_empty, hmac_running, digest_valid.\n- Interrupts: intr_hmac_done, intr_fifo_empty, intr_hmac_err.\n- Alert: fatal_fault (bus intg errors, etc.)\n\nSEC_CM annotations in OpenTitan HMAC:\n- CONFIG.SEC_WIPE: key wipe via WIPE_SECRET\n- BUS.INTEGRITY: TL-UL bus integrity\n- Actually the hmac.hjson has: \n  - `BUS.INTEGRITY` \n  - `CONFIG.SEC_WIPE` for key? Let me recall... The hmac.hjson lists:\n    - main clock, etc.\n    - SEC_CM: [\"BUS.INTEGRITY\"] — I think HMAC only has BUS.INTEGRITY. Hmm, but the wipe_secret is a security feature too.\n\nActually in OpenTitan, hmac.hjson has:\n```\n  // SEC_CM: BUS.INTEGRITY\n```\nAnd the key wipe is part of the design. Let me think about what's in the actual hmac.hjson:\n\n```\n{\n  name: \"hmac\",\n  ...\n  // SEC_CM: BUS.INTEGRITY\n  ...\n}\n```\n\nHmm, I recall HMAC has `SEC_CM: BUS.INTEGRITY` only. But the task gives us wipe_secret as a security mechanism.\n\nThe task says: \"针对每个安全敏感数据路径（密钥/种子/掩码/摘要/擦除/状态机/总线/中断），提出运行时可检查的不变量。\"\n\nSo I should propose invariants for:\n- 密钥: reg_core_consistent, wipe_clears\n- 摘要: reg_core_consistent, access_control (read only when idle/digest_valid)\n- 擦除: wipe_clears\n- 状态机: fsm_sparse_encoding (hmac_core FSM states)\n- 总线: bus_intg_check\n- 中断: interrupt_first_event\n- 访问控制: cfg_block_gating (KEY_REGWEN, CFG_REGWEN)\n- read_only_leak: KEY registers are write-only, read must return 0\n\nLet me design the invariants:\n\n1. **key_wipe_clears_core_key** (wipe_clears)\n   - signal: `u_dut.u_hmac_core.secret_key` (or `u_dut.key_0` etc.)\n   - trigger: write WIPE_SECRET with any data (e.g., 0xDEADBEEF)\n   - After writing WIPE_SECRET, the core's secret key must be all zeros.\n   - In OpenTitan hmac_core.sv, there's `secret_key` reg: `logic [1023:0] secret_key;` and wipe clears it: when `reg2hw.wipe_secret.qe`, `secret_key <= '0`. Actually let me recall hmac_core.sv:\n\n```systemverilog\n  // SEC_CM: KEY.SEC_WIPE\n  always_ff @(posedge clk_i or negedge rst_ni) begin\n    if (!rst_ni) begin\n      secret_key <= '0;\n    end else if (reg2hw.wipe_secret.qe) begin\n      secret_key <= '0;\n    end else if (...) begin\n      // load from reg2hw.key\n    end\n  end\n```\n\nHmm, actually in hmac_core.sv the key is stored as `secret_key` and there's also `key_0`... Let me recall. In hmac_core.sv:\n\n```systemverilog\n  logic [1023:0] secret_key;\n  ...\n  always_ff @(posedge clk_i or negedge rst_ni) begin\n    if (!rst_ni) begin\n      secret_key <= '0;\n    end else if (reg2hw.wipe_secret.qe) begin\n      secret_key <= '0;\n    end else begin\n      for (int i = 0 ; i < 8 ; i++) begin\n        if (reg2hw.key[i].qe) begin\n          secret_key[32*i +: 32] <= reg2hw.key[i].q;\n        end\n      end\n    end\n  end\n```\n\nSomething like that. Actually the key is 1024 bits (32 words × 32 bits). The comment says `input [1023:0] secret_key_i, // {word0, word1, ..., word7}` — hmm, that says 8 words but 1024 bits = 32 words. The comment in the given snippet says `{word0, word1, ..., word7}` which is inconsistent, but whatever — the signal is 1024 bits.\n\nWait, the given snippet: `rtl/hmac_core.sv:11: input [1023:0] secret_key_i, // {word0, word1, ..., word7}`. Hmm, 1024 bits = 32 words of 32 bits. The comment might be from an older version. Anyway.\n\nActually wait — in hmac_core.sv, secret_key_i is an input to hmac_core from the top-level hmac.sv, which connects it to the key registers stored in hmac.sv? Let me recall hmac.sv:\n\nIn hmac.sv:\n```systemverilog\n  logic [1023:0] secret_key;\n  // SEC_CM: KEY.SEC_WIPE\n  always_ff @(posedge clk_i or negedge rst_ni) begin\n    if (!rst_ni) begin\n      secret_key <= '0;\n    end else if (reg2hw.wipe_secret.qe) begin\n      secret_key <= '0;\n    end else begin\n      for (int i = 0; i < 32; i++) begin\n        if (reg2hw.key[i].qe) begin\n          secret_key[32*i +: 32] <= reg2hw.key[i].q;\n        end\n      end\n    end\n  end\n  assign secret_key_i = secret_key; // to hmac_core\n```\n\nHmm, actually I think the key storage is in hmac.sv (top) and hmac_core receives it as input. The wipe_secret clears it. Also, after each hash operation completes... Actually there's also wiping of the key when the operation is done? No — the key persists until wiped by software or reset.\n\nAlso in hmac_core, i_pad/o_pad are computed from secret_key. And there's `shaf_rdata` etc.\n\nOK for the invariant, the signal could be `u_dut.secret_key` (in hmac.sv) or `u_dut.u_hmac_core.secret_key_i`. I'll use `u_dut.u_hmac_core.secret_key_i` or `u_dut.secret_key`. Let me use `u_dut.secret_key` since that's the storage.\n\n2. **key_regs_read_as_zero** (read_only_leak)\n   - KEY_0..KEY_31 are write-only. Reading them must return 0.\n   - signal: `u_dut.reg2hw.key[0].q` or the readback path `u_dut.u_reg.hw2reg.key[0].d` — hmm. For read-only leak, the check is on the bus read data: reading KEY_0 must return 0.\n   - signal: `u_dut.u_reg.hw2reg.key[0].d` (should be 0) or the TL read data.\n   - trigger: write KEY_0 = 0xDEADBEEF, then read KEY_0 → must be 0.\n   - Actually in OpenTitan, KEY registers are `sw: wo` so reads return 0. The hw2reg key d is not connected (0).\n\n3. **key_regwen_blocks_key_write** (cfg_block_gating)\n   - KEY_REGWEN: when KEY_REGWEN.EN is 0, writes to KEY_0..31 must be blocked.\n   - signal: `u_dut.reg2hw.key[0].qe` must not pulse when key_regwen is 0.\n   - trigger: write KEY_REGWEN = 0x0 (disable), then write KEY_0 = 0x12345678 → key qe must not fire / secret_key must not change.\n   - In hmac.sv, there's `key_wen` logic: writes to key allowed only when `reg2hw.key_regwen.q` is set... Actually the regwen is handled in reg_top: `key_0_we = ... & key_regwen_qs`. Hmm, in the generated reg_top, the regwen gating is applied: `assign key_0_we = addr_hit[...] & reg_we & !reg_error & key_regwen_qs;` something like that. Actually for hmac, KEY_REGWEN gates KEY writes in reg_top.\n\n4. **cfg_regwen_blocks_cfg_write** (cfg_block_gating)\n   - CFG_REGWEN: when 0, writes to CFG blocked.\n   - Actually in hmac, CFG_REGWEN is set to 0 when sha_en... hmm. Let me recall: CFG_REGWEN.EN is 1 by default; when HMAC is not idle (operation started), CFG_REGWEN is cleared. Writes to CFG blocked when not idle.\n   - signal: `u_dut.reg2hw.cfg.hmac_en.qe` etc.\n\n5. **wipe_clears_msg_fifo** — Actually WIPE_SECRET also wipes... hmm, in OpenTitan HMAC, WIPE_SECRET wipes the key only? Let me check. From hmac.hjson:\n\n```\n    { name: \"WIPE_SECRET\",\n      desc: '''Any write to this register clears the secret key and the message FIFO.\n            '''\n      ...\n    }\n```\n\nHmm, I believe WIPE_SECRET clears the secret key. Does it clear the message FIFO? Let me think... In hmac.sv:\n\n```systemverilog\n  // FIFO control\n  assign fifo_wdata = ...;\n  ...\n  // wipe\n  assign msg_fifo_wipe = reg2hw.wipe_secret.qe;\n```\n\nHmm. Actually I recall in hmac.sv:\n\n```systemverilog\n  assign fifo_clr = reg2hw.wipe_secret.qe;\n```\n\nLet me think about the actual code. From OpenTitan hmac.sv (recent):\n\n```systemverilog\n  // Interrupts\n  ...\n  assign hmac_err = ...;\n\n  // FIFO\n  prim_fifo_sync #(...) u_msg_fifo (\n    ...\n    .clr_i   (reg2hw.wipe_secret.qe),\n    ...\n  );\n```\n\nHmm, I'm not 100% sure. Let me check the actual OpenTitan hmac.sv from memory:\n\n```systemverilog\n  prim_fifo_sync #(\n    .Width (FifoDWidth),\n    .Pass  (0),\n    .Depth (MsgFifoDepth)\n  ) u_msg_fifo (\n    .clk_i,\n    .rst_ni,\n    .clr_i   (reg2hw.wipe_secret.qe),\n    .wvalid_i(fifo_wvalid & fifo_wready),\n    .wready_o(fifo_wready),\n    .wdata_i (fifo_wdata),\n    .depth_o (msg_depth)\n  );\n```\n\nYes! I'm fairly confident WIPE_SECRET clears the message FIFO too. From hmac.hjson WIPE_SECRET description: \"Any write to this register clears the secret key and the message FIFO\". Yes, I believe that's right.\n\nSo invariant: writing WIPE_SECRET clears both secret_key and msg FIFO.\n\n6. **digest_read_only_when_valid** (access_control)\n   - Reading DIGEST registers while HMAC not idle (or digest not valid) triggers error sw_read_digest_without_idle and ERR_CODE set.\n   - Actually the rule: reading digest when not idle → error. The invariant could be err_code_coherent: after an illegal digest read, ERR_CODE must be set.\n   - Or access_control: digest read while busy must be blocked/errored.\n\n7. **err_code_coherent**: after sw_read_digest_without_idle error, ERR_CODE register must show the error code.\n\n8. **fsm_sparse_encoding**: hmac_core's SHA-2 FSM uses sparse encoding. In hmac_core.sv / sha2.sv, the FSM states: Idle, MsgInit? Let me recall sha2.sv:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    Idle, Init, Msg, Comp, Done\n  } sha2_st_e;\n  sha2_st_e sha2_st, sha2_st_d;\n```\n\nHmm, in sha2.sv the state encoding... Actually OpenTitan uses prim_sparse_fsm_flop for FSMs with sparse encoding. In hmac_core.sv:\n\n```systemverilog\n  // SEC_CM: FSM.SPARSE\n  typedef enum logic [2:0] {\n    HMAC_IDLE    = 3'b001,\n    HMAC_MSG     = 3'b010,\n    HMAC_IPAD    = 3'b100,\n    HMAC_OPAD    = 3'b... \n  } hmac_state_e;\n```\n\nHmm. Let me recall hmac_core.sv:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    Idle,\n    Init,\n    IPad,\n    Msg,\n    OPad\n  } hmac_state_e;\n```\n\nActually from the actual OpenTitan code (hmac_core.sv):\n\n```systemverilog\n  // FSM states\n  typedef enum logic [2:0] {\n    HMAC_IDLE = 3'b001,\n    HMAC_MSG  = 3'b010,\n    HMAC_IPAD = 3'b100,\n    HMAC_OPAD = 3'b... \n```\n\nHmm, I don't remember exactly. Let me think about what's in hmac_core.sv:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    Idle,\n    Init,\n    IPad,\n    Msg,\n    OPad\n  } hmac_state_e;\n\n  hmac_state_e hmac_state, hmac_state_d;\n```\n\nAnd there's `// SEC_CM: FSM.SPARSE` maybe with prim_sparse_fsm_flop. In recent OpenTitan, they use:\n\n```systemverilog\n  prim_sparse_fsm_flop #(\n    .StateEnumT(hmac_state_e),\n    .Width(3),\n    .ResetValue({HMAC_IDLE})\n  ) u_state_regs (\n    .clk_i,\n    .rst_ni,\n    .state_i (hmac_state_d),\n    .state_o (hmac_state)\n  );\n```\n\nHmm, actually I recall in hmac_core.sv:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    HMAC_IDLE    = 3'b001,\n    HMAC_MSG     = 3'b010,\n    HMAC_IPAD    = 3'b100,\n    HMAC_OPAD    = 3'b... \n```\n\nI'm not sure of exact encodings. For the invariant, I can just say the FSM state signal `u_dut.u_hmac_core.hmac_state` must always be one of the legal sparse encodings. The checker would verify `state in {legal set}` at every step.\n\nActually, let me look at the real code. OpenTitan hmac_core.sv (from master):\n\n```systemverilog\nmodule hmac_core (\n  input clk_i,\n  input rst_ni,\n\n  input [1023:0] secret_key_i,\n\n  input        hmac_en_i,\n  input        hash_start_i,\n  input        hash_process_i,\n  output logic hash_start_ready_o,\n  output logic hash_done_o,\n\n  input        shaf_rvalid_i,\n  input  [63:0] shaf_rdata_i,\n  output logic shaf_rready_o,\n\n  output logic        fifo_wvalid_i,\n  ...\n);\n```\n\nHmm, the given snippet shows `input [1023:0] secret_key_i, // {word0, word1, ..., word7}` at line 11. OK.\n\nAnd the FSM:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    HMAC_IDLE = 3'b001,\n    HMAC_MSG  = 3'b010,\n    HMAC_IPAD = 3'b100,\n    HMAC_OPAD = 3'b... \n```\n\nHmm wait. Let me recall from the actual source:\n\n```systemverilog\n  // FSM\n  typedef enum logic [2:0] {\n    Idle,\n    Init,\n    IPad,\n    Msg,\n    OPad\n  } hmac_state_e;\n```\n\nI've seen this in hmac_core.sv. The states are Idle, Init, IPad, Msg, OPad — 5 states, 3 bits. With SEC_CM: FSM.SPARSE, they'd be encoded sparsely. In recent OpenTitan:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    HMAC_IDLE = 3'b001,\n    HMAC_INIT = 3'b010,\n    HMAC_IPAD = 3'b100,\n    HMAC_MSG  = 3'b... \n```\n\nHmm, 5 states in 3 bits with one-hot-ish: 001, 010, 100, 011, 101? Not sure.\n\nFor the invariant, I don't need exact encodings — I can specify the signal and say \"must be one of the legal sparse encodings defined in hmac_state_e\". The checker presumably knows how to check sparse encoding (one-hot or from enum). I'll write the signal as `u_dut.u_hmac_core.u_state_regs.state_o` or `u_dut.u_hmac_core.hmac_state`.\n\nLet me be careful: the invariant format wants \"signal\": white-box signal name. For FSM sparse encoding, signal = `u_dut.u_hmac_core.hmac_state`.\n\n9. **bus_intg_check**: TL-UL integrity error → alert. In hmac, the reg_top checks intg via prim_tlul_lc_gate? Actually hmac reg_top has `tlul_reg_intg`? The bus integrity is checked in the adapter (tlul_adapter_reg) and errors propagate as intg_error → alert_fatal_fault via alert senders. Signal: `u_dut.u_reg.intg_error` or `u_dut.alert_test`... For hmac, there's a fatal alert? Let me recall hmac.hjson alert list: hmac has `fatal_fault` alert? Hmm.\n\nActually, OpenTitan HMAC hjson:\n\n```\n  alert_list: [\n    { name: \"fatal_fault\", ... }\n  ]\n```\n\nHmm, I don't think HMAC has an alert. Let me think... HMAC is a peripheral; with BUS.INTEGRITY SEC_CM, the intg error... In many peripherals, intg errors on TL-UL trigger a fatal alert only if the IP has alert infrastructure. HMAC... Let me recall the hmac.hjson:\n\n```\n{\n  name: \"hmac\",\n  ...\n  alert_list: [\n    { name: \"fatal_fault\",\n      desc: '''This fatal alert is triggered when a fatal TL-UL bus integrity fault is detected.''',\n    }\n  ],\n```\n\nHmm, I believe newer versions of HMAC do have a fatal_fault alert. Let me think... Yes, I'm fairly confident modern OpenTitan HMAC has:\n\n```\n  // SEC_CM: BUS.INTEGRITY\n  ...\n  alert_list: [\n    { name: \"fatal_fault\", ... }\n  ]\n```\n\nActually, checking my memory of hmac.hjson:\n\n```\n  alert_list: [\n    { name: \"fatal_fault\",\n      desc: '''This fatal alert is triggered when a fatal TL-UL bus integrity fault is detected inside the HMAC unit.\n            '''\n    }\n  ]\n```\n\nYes, I believe that's right. The signal would be `u_dut.u_reg.intg_error` or in hmac.sv: `assign fatal_alert = ...`? Hmm. In hmac.sv:\n\n```systemverilog\n  logic intg_error;\n  ...\n  // SEC_CM: BUS.INTEGRITY\n  assign hw2reg.fatal_fault... \n```\n\nHmm, actually hmac.sv might not have fatal_fault. Let me think about what signals exist. In hmac_reg_top.sv generated code, there's `intg_error` output from tlul_adapter_reg... The reg_top has:\n\n```systemverilog\n  tlul_adapter_reg #(\n    .RegAw(AW),\n    .RegDw(DW)\n  ) u_reg_if (\n    ...\n    .intg_err_o (intg_err)\n  );\n```\n\nHmm, older versions: `.intg_err_o (intg_err[0])` or similar. And then `assign tl_o.d_error = ...`? Actually intg_err goes to both d_error and alert.\n\nFor the invariant, I'll use signal `u_dut.u_reg.u_reg_if.intg_err` or simply `u_dut.intg_error`. To be safe, I'll write `u_dut.u_reg.intg_error` — hmm.\n\nLet me just use plausible hierarchical names. The checker is hypothetical; the important thing is the invariant semantics. I'll use names consistent with OpenTitan conventions:\n\n- `u_dut.u_reg.u_reg_if.intg_err` — the TLUL adapter integrity error.\n- Or `u_dut.u_reg.intg_error`.\n\nI'll go with `u_dut.u_reg.u_reg_if.intg_err_o`? Hmm, output port. Let me use `u_dut.u_reg.intg_error` — hmm.\n\nActually in generated hmac_reg_top.sv, there's:\n\n```systemverilog\n  logic intg_err;\n  ...\n  tlul_adapter_reg #(\n    .RegAw(AW),\n    .RegDw(DW)\n  ) u_reg_if (\n    .clk_i,\n    .rst_ni,\n\n    .tl_i        (tl_i),\n    .tl_o        (tl_o_reg),\n\n    .we_o        (reg_we),\n    .re_o        (reg_re),\n    .addr_o      (reg_addr),\n    .wdata_o     (reg_wdata),\n    .be_o        (reg_be),\n    .busy_i      (reg_busy),\n    .rdata_i     (reg_rdata),\n    .error_i     (reg_error),\n\n    // Bus integrity check\n    .intg_err_o  (intg_err[0])  // or (intg_err)\n  );\n```\n\nHmm, in newer versions it's `.intg_err_o (intg_err[0])` with `logic [NumAlerts-1:0] intg_err;`? No wait, that's for alert... Let me recall:\n\n```systemverilog\n  // Since there are no outgoing integrity signals for registers, the intg_err\n  // is fed into the fatal alert.\n  logic [NumAlerts-1:0] intg_err;\n  ...\n  tlul_adapter_reg #(\n    ...\n    .intg_err_o (intg_err[0])\n  );\n```\n\nHmm, no. Actually in recent reg_top generation:\n\n```systemverilog\n  logic intg_err;\n  ...\n  ) u_reg_if (\n    ...\n    .intg_err_o (intg_err)\n  );\n\n  // alerts\n  assign alert_tx_o[0].p = fatal_err... \n```\n\nHmm, I recall in reg_top:\n\n```systemverilog\n  assign intg_err_ecc = ...;\n  ...\n  always_ff @(posedge clk_i or negedge rst_ni) begin\n    if (!rst_ni) begin\n      intg_err_q <= '0;\n    end else if (intg_err) begin\n      intg_err_q <= 1'b1;\n    end\n  end\n  ...\n  assign alert_test = {...};\n  assign hw2reg.fatal_fault.d = 1'b1;\n  assign hw2reg.fatal_fault.de = intg_err | intg_err_q;\n```\n\nSomething like that. The exact structure: reg_top has `intg_err` that latches and drives `fatal_fault` alert cause and the alert sender.\n\nFor the invariant, signal: `u_dut.u_reg.intg_err` (or the alert cause `u_dut.u_reg.hw2reg.fatal_fault.de`). I'll use `u_dut.u_reg.intg_err`.\n\nHmm, but wait — does HMAC actually have fatal_fault alert? Let me double check by recalling the hmac.hjson from OpenTitan master:\n\n```\n// Copyright lowRISC contributors (OpenTitan project).\n// Licensed under the Apache License, Version 2.0, see LICENSE for details.\n// SPDX-License-Identifier: Apache-2.0\n{ name: \"hmac\",\n  ...\n  // SEC_CM: [\"BUS.INTEGRITY\"]\n  ...\n  alert_list: [\n    { name: \"fatal_fault\",\n      desc: '''This fatal alert is triggered when a fatal TL-UL bus integrity fault is detected.\n            '''\n    }\n  ],\n```\n\nI'm fairly confident HMAC has a fatal_fault alert in recent versions. Yes — HMAC got BUS.INTEGRITY and fatal alert. OK.\n\nAlternatively, to be safe, I can frame the bus_intg_check invariant on the reg_top's intg_err signal and say it must trigger the fatal alert / error response. The rule bus_intg_check says: \"TL-UL intg 错误必须触发 alert 或 error\". So signal could be `u_dut.u_reg.intg_err` and check that when corrupted write occurs, alert fires.\n\n10. **interrupt_first_event**: hmac_done interrupt fires once per hash completion. If hash_done... The interrupt bit stays until cleared by SW; the \"first event\" semantics: intr_hmac_done asserts once when hash completes and doesn't re-assert until cleared. Signal: `u_dut.intr_hmac_done_o` or `u_dut.hw2reg.intr_state.hmac_done.de`.\n\nTrigger: write KEY, CFG (hmac_en=1, sha_en=1), MSG_FIFO data, CMD (hash_start=1), MSG_FIFO more data, CMD (hash_process=1) → wait for done → intr_hmac_done must pulse exactly once.\n\n11. **reg_core_consistent**: key written via KEY_0..31 registers must match core's secret_key. Trigger: write KEY_0=0x..., ..., KEY_31=0x...; then check `u_dut.secret_key == written value`. This is a white-box check.\n\nAlso digest consistency: digest computed in sha2 (u_dut.u_hmac_core.u_sha2.digest) vs DIGEST registers after done. reg_core_consistent: DIGEST_0..15 readback must equal core digest. Signal: `u_dut.u_hmac_core.u_sha2.digest[0]` vs `u_dut.hw2reg.digest[0].d`. Hmm, that's a cross-signal consistency. The rule reg_core_consistent: \"同一数据在 reg 侧和 core 侧的副本必须一致\". So for digest: after hash done, DIGEST regs must equal sha2 digest.\n\n12. **wipe_clears on reset**: after reset, secret_key must be 0. That's covered by wipe_clears with trigger = reset? The trigger_regs are register writes; reset isn't a register write. But wipe via WIPE_SECRET covers it.\n\n13. **status/digest_valid coherence**: digest_valid in STATUS must be 1 after hash done, and cleared on new start. Could be err_code_coherent or a state invariant. Maybe skip or include as access_control.\n\n14. **msg_fifo read while busy error**: reading MSG_FIFO while not idle → error sw_read_message_without_idle → ERR_CODE set. err_code_coherent.\n\n15. **sha_disabled error**: starting hash with sha_en=0 → error sw_sha_disabled → ERR_CODE. err_code_coherent.\n\n16. **sw_invalid_key_length**: if key length < 16 bytes... Actually HMAC requires key >= block size? The error sw_invalid_key_length occurs when hash_start with hmac_en and key length invalid. Hmm, in OpenTitan HMAC, key length is fixed 1024-bit? There's no key length config in the current design... Actually there was `sw_invalid_key_length` error in ERR_CODE. Let me recall ERR_CODE fields:\n\n```\n  ERR_CODE: \n    bit 0: sw_invalid_key_length\n    bit 1: sw_read_digest_without_idle\n    bit 2: sw_read_message_without_idle\n    bit 3: sw_sha_disabled\n```\n\nYes, that's the ERR_CODE register in hmac.hjson. These are set on software errors.\n\nOK now, the task: \"针对每个安全敏感数据路径（密钥/种子/掩码/摘要/擦除/状态机/总线/中断），提出运行时可检查的不变量。\" — for each security-sensitive data path (key/seed/mask/digest/wipe/FSM/bus/interrupt), propose runtime-checkable invariants.\n\nHMAC doesn't have masks/entropy/PRNG (no changes_across_runs applicable — there's no randomness in HMAC). I should not force changes_across_runs since there's no random signal. Actually, could I? HMAC has no PRNG. Skip it. The instructions say propose invariants for each path present; HMAC lacks seed/mask paths. I'll cover: key, digest, wipe, FSM, bus, interrupt, access control (regwen), error code, read-only leak.\n\nLet me now write the invariants with proper trigger_regs and signals:\n\n**Invariant 1: key_wipe_clears_secret**\n- rule: wipe_clears\n- signal: `u_dut.secret_key` (1024-bit key storage in hmac.sv) — or `u_dut.u_hmac_core.secret_key_i`. I'll use `u_dut.secret_key`.\n- trigger_regs: [{\"reg\": \"KEY_0\", \"data\": \"0xDEADBEEF\"}, ..., {\"reg\": \"WIPE_SECRET\", \"data\": \"0xA5A5A5A5\"}]\n- rationale: WIPE_SECRET 写入后核心密钥必须归零，防止密钥残留泄露 (SEC_CM: KEY.SEC_WIPE)。\n\nActually, to make the check meaningful, first write some key words, then wipe, then check secret_key == 0. The trigger_regs list: KEY_0 write, KEY_1 write, WIPE_SECRET write. The checker checks after the sequence that signal == 0.\n\n**Invariant 2: msg_fifo_wipe_clears**\n- rule: wipe_clears\n- signal: `u_dut.u_msg_fifo.fifo_data` or `u_dut.msg_depth`? The FIFO depth must go to 0 after wipe. Signal: `u_dut.msg_depth` (hmm, in hmac.sv the fifo depth output is `msg_depth`). Actually in hmac.sv:\n\n```systemverilog\n  prim_fifo_sync #(...) u_msg_fifo (\n    ...\n    .depth_o (msg_depth)\n  );\n```\n\nHmm, I think there's `wdata` stored... The FIFO internal storage: `u_msg_fifo.u_fifo.fifo_q`? For prim_fifo_sync with Depth>1, it's `u_msg_fifo.u_fifo` (prim_fifo_sync_cntx?). Simpler: use STATUS.fifo_empty — after wipe, STATUS.fifo_empty must be 1. But invariant signal should be white-box: `u_dut.msg_depth` hmm. Actually STATUS.fifo_empty is derived from msg_depth == 0. Let me use signal `u_dut.u_msg_fifo.depth_o`? Hmm, that's a port. \n\nLet me just use `u_dut.msg_depth` — hmm, I'm not sure that's the exact name. In hmac.sv:\n\n```systemverilog\n  logic [MsgFifoDepthW-1:0] msg_depth;\n```\n\nYes, I believe `msg_depth` exists in hmac.sv. OK, signal: `u_dut.msg_depth`.\n\nHmm wait, actually let me reconsider whether WIPE_SECRET clears the msg FIFO. From hmac.hjson:\n\n```\n    { name: \"WIPE_SECRET\",\n      desc: '''Any write to this register clears the secret key and the message FIFO.\n            '''\n      swaccess: \"wo\",\n      hwaccess: \"hrw\",\n      hwqe:     \"true\",\n      fields: [\n        { bits: \"31:0\", name: \"WIPE_SECRET\", desc: \"Write any value to wipe secrets.\" }\n      ],\n    }\n```\n\nYes, I'm now quite confident: \"Any write to this register clears the secret key and the message FIFO.\" So both invariants are valid.\n\n**Invariant 3: key_write_only_reads_zero**\n- rule: read_only_leak\n- signal: `u_dut.u_reg.hw2reg.key[0].d` — hmm, in reg_top, the read data for KEY comes from hw2reg.key[i].d which is tied 0. Actually since KEY is wo, the reg_top returns 0 for reads. The white-box signal: `u_dut.u_reg.u_reg_if.rdata_i`? Too indirect. \n\nBetter: the check is a bus-level readback: sig_read of the TL response data? Hmm. The action set is write/step/sig_read. For read_only_leak, the natural check: after writing KEY_0=0xDEADBEEF, perform sig_read on the register readback path and assert 0. The white-box signal for KEY_0 read data: in hmac_reg_top.sv, `reg_rdata` mux: for addr_hit of KEY_0, `reg_rdata` gets `hw2reg.key[0].d`? For wo registers, the generated code assigns `hw2reg.key[0].d = '0;`? Hmm, actually for wo registers, reg_top doesn't include them in the read mux — it returns 0. Let me recall: in reg_top, the read data mux includes only registers with swaccess ro/rw. For wo, the case item... Actually the generated reg_top has:\n\n```systemverilog\n    unique case (1'b1)\n      addr_hit[0]: begin\n        reg_rdata = ...; // KEY_0 — for wo, returns 0? \n```\n\nHmm, in OpenTitan generated reg_top, wo registers still appear in the case but with `reg_rdata = 32'h0`? Let me think. The reg_top generates:\n\n```systemverilog\n  always_comb begin\n    reg_rdata = '0;\n    ...\n    unique case (1'b1)\n      addr_hit[0]: begin\n        reg_rdata = key_0_qs;  // hmm, but wo has no qs\n      end\n```\n\nFor wo registers, there's no `qs` (no storage for readback). The generated code for wo: the addr_hit case assigns... Actually I recall for wo registers, reg_top doesn't create `key_0_qs`; the read returns 0. In the case statement, wo registers are simply not listed, or listed with 0. Hmm.\n\nIn any case, the semantic invariant: reading KEY_0..31 returns 0. The white-box signal could be `u_dut.u_reg.u_reg_if.rdata_i` at the time of read, or the final TL data. Simplest: signal = `u_dut.u_reg.reg_rdata` hmm, but that's the mux output before adapter. Hmm, but reg_rdata for KEY address should be 0.\n\nAlternatively, use `u_dut.u_reg.hw2reg.key[0].d` — in hmac.sv, hw2reg.key is not driven (default 0). Hmm, actually in hmac.sv, is there `hw2reg` for key? No — key is wo, hw doesn't return it. In reg_top, `hw2reg.key[0].d` would be tied to 0 in the reg_top itself? The reg_top instantiates `hmac_reg_top` which has hw2reg input from hmac.sv. For wo fields, reg_pkg hw2reg struct may not even have key field. Right — wo registers don't appear in hw2reg.\n\nOK let me pick a different signal: the read data returned on the bus for KEY addresses. In reg_top: `reg_rdata` mux output. For KEY addresses, since wo, the generated code... Let me actually recall the generated hmac_reg_top.sv read mux:\n\n```systemverilog\n  always_comb begin\n    reg_rdata = '0;\n    ...\n    unique case (1'b1)\n      addr_hit[0]: begin\n        reg_rdata = key_0_qs;\n      end\n```\n\nHmm no. For wo registers, reg_pkg... OK here's the thing: in OpenTitan reg_top generation, for `swaccess: wo`, there is no `qs` signal and the read returns 0. The generated case statement includes the address hit but assigns... Let me recall an actual example, e.g., alert_handler or hmac. In hmac_reg_top.sv:\n\n```systemverilog\n      addr_hit[3]: begin\n        reg_rdata = [31:0] ... \n```\n\nI genuinely recall that for wo registers, the generated code does:\n\n```systemverilog\n      addr_hit[2]: begin\n        reg_rdata = [31:0] 32'h0;\n      end\n```\n\nHmm, not sure. Actually I think the reg_top tool generates for wo: the register doesn't appear in the read mux at all, so reg_rdata stays 0 (default). Either way, reading KEY returns 0.\n\nFor the invariant signal, I'll use `u_dut.u_reg.reg_rdata` with the check \"when reading KEY_0 address, returned data must be 0\". Hmm, but sig_read of reg_rdata requires the read to happen. The trigger: write KEY_0, then the checker reads (sig_read triggers a bus read?). The actions are write(reg), step(clock), sig_read(white-box signal). sig_read is white-box signal read, not bus read. So to check \"read returns 0\", the checker would need to observe the bus response... \n\nAlternative approach: check the storage element. KEY has no storage in reg (wo). The value written goes reg2hw.key[i].q → hmac.sv secret_key. The \"leak\" concern: if KEY were accidentally readable, secret would leak. The white-box check: the readback path `u_dut.u_reg.reg_rdata` when addr_hit on KEY must be 0. I'll phrase signal as `u_dut.u_reg.reg_rdata` and rationale: KEY registers are write-only; readback must return 0 to prevent key leakage via bus readback.\n\nHmm, but reg_rdata is only meaningful during a read. The checker can do: write KEY_0, then... it can't issue a bus read with the given actions. Unless sig_read of reg_rdata is done while the checker itself triggers a read? The action list: write(寄存器), step(时钟), sig_read(白盒信号). So sig_read just samples a signal. To check readback, the checker would need to perform a register read — maybe sig_read can target a register too? \"sig_read(白盒信号)\" — white-box signals only.\n\nHmm. Maybe better to model read_only_leak differently: the check is that no path exists from key storage to read data — but runtime, we can check that during any read transaction to KEY address, reg_rdata == 0. The trigger could be a write to KEY_0 followed by... hmm.\n\nAlternatively, I can define the invariant on the reg_top's read mux constant: signal `u_dut.u_reg.reg_rdata` checked during a dummy read... The checker framework presumably can issue reads as part of sig_read? Not per spec.\n\nLet me reconsider: maybe for read_only_leak, the trigger is a write to KEY_0 and the check is on the hw2reg path... but there is no hw2reg for key.\n\nOK, alternative approach: many checkers implement read_only_leak by having the testbench issue a read (sig_read can include reading the register file through the bus? no...). \n\nHmm, let me look at the rule description again: \"read_only_leak: write-only 寄存器读回必须全 0（信息泄露）\". The check: readback of write-only register must be all 0. The mechanism to read back: presumably the checker can do a bus read via... only write/step/sig_read available. Maybe sig_read can be applied to the reg_top's internal read-data mux while the checker drives a read? I think practically, the checker would: write KEY_0=0xDEADBEEF (trigger), then perform a \"read\" — but reads aren't in the action list. \n\nHmm, but maybe the framework treats sig_read on a register's readback signal as performing the read. I'll just specify the signal as the readback data path and note in rationale that readback must be 0. The signal: `u_dut.u_reg.reg_rdata` — during a read of KEY_0 offset, must be 0.\n\nActually, you know what — I could make the signal more precise: in hmac_reg_top.sv, the read mux for KEY addresses. Since KEY is wo, generated reg_top returns 0. The signal `u_dut.u_reg.reg_rdata` is the mux output. I'll go with that.\n\n**Invariant 4: key_regwen_gates_key_writes**\n- rule: cfg_block_gating\n- signal: `u_dut.reg2hw.key[0].qe` (write-enable pulse to core) — when KEY_REGWEN.EN=0, writing KEY_0 must not produce qe pulse / must not change secret_key.\n- trigger_regs: [{\"reg\": \"KEY_REGWEN\", \"data\": \"0x0\"}, {\"reg\": \"KEY_0\", \"data\": \"0x12345678\"}]\n- check: after sequence, `u_dut.secret_key[31:0]` unchanged (still 0 or previous). Actually the gating happens in reg_top: `key_0_we = addr_hit & reg_we & !reg_error & key_regwen_qs;`? Hmm — wait, does reg_top gate key writes with regwen? In OpenTitan, REGWEN registers gate writes in reg_top: e.g., `assign key_0_we = addr_hit[3] && reg_we && !reg_error && key_regwen_qs;` Hmm, actually I recall for hmac:\n\n```systemverilog\n  assign key_0_we = addr_hit[3] && reg_we && !reg_error && key_regwen_qs;\n```\n\nHmm, not exactly — the regwen might be applied in the IP (hmac.sv) rather than reg_top. Let me think. In hmac_reg_top.sv generated code:\n\n```systemverilog\n  assign key_0_we = addr_hit[ 9] && reg_we && !reg_error;\n```\n\nand the regwen gating... For hmac, KEY_REGWEN gates KEY writes. Where? Looking at hmac.hjson:\n\n```\n    { name: \"KEY_REGWEN\",\n      desc: \"Key write enable. Key write is only allowed when this register is 1.\",\n      swaccess: \"ro\",\n      hwaccess: \"hro\",\n      ...\n      tags: [// This regwen is completely under HW management, and thus cannot be\n             // manipulated by software.\n             \"excl:CsrNonInitTests\"]\n    },\n```\n\nHmm wait, KEY_REGWEN for hmac: swaccess ro, hwaccess hrw — HW clears it when operation starts. And the gating is done in reg_top: `key_0_we = ... & key_regwen_qs`. Hmm, actually for regwen registers with `regwen: \"KEY_REGWEN\"` property, reg_top generates:\n\n```systemverilog\n  assign key_0_we = addr_hit[9] && reg_we && !reg_error && key_regwen_qs;\n```\n\nHmm, I don't think reg_top does that automatically... Actually yes it does! The regwen mechanism: registers with `regwen: \"KEY_REGWEN\"` get their `_we` gated by the regwen register's qs in reg_top. Let me verify with an actual OpenTitan reg_top, e.g., hmac_reg_top.sv:\n\n```systemverilog\n  assign key_0_we = addr_hit[ 9] && reg_we && !reg_error && key_regwen_qs;\n```\n\nHmm... I actually recall that in hmac_reg_top.sv, the key we is:\n\n```systemverilog\n  assign key_0_we = addr_hit[ 9] && reg_we && !reg_error;\n```\n\nand the regwen gating is in hmac.sv? No wait. Let me think about how regtool handles regwen. In the generated reg_top, for a register with regwen, the `_we` signal is ANDed with the regwen qs:\n\nFrom actual generated code (e.g., flash_ctrl or hmac):\n\n```systemverilog\n  assign key_0_we = addr_hit[ 9] && reg_we && !reg_error && key_regwen_qs;\n```\n\nHmm, I believe I've seen exactly this pattern in hmac_reg_top.sv. Let me check the given snippet: line 2128: `assign wipe_secret_we = (addr_hit[8] && reg_we && reg_error);` — wait, that says `reg_error`?! That's odd. `wipe_secret_we = (addr_hit[8] && reg_we && reg_error)`. Hmm, that must be a typo in the problem statement (should be `!reg_error`). Whatever — the given code has `reg_error` but real code has `!reg_error`. I'll not dwell.\n\nOK so for the regwen gating: in hmac_reg_top.sv, I'm fairly confident it's:\n\n```systemverilog\n  assign key_0_we = addr_hit[ 9] && reg_we && !reg_error && key_regwen_qs;\n```\n\nHmm, wait. Actually no! Let me think again. In OpenTitan, the regwen gating for KEY registers in HMAC is done in hmac.sv, not reg_top? Let me recall hmac.sv:\n\n```systemverilog\n  // Key write enable\n  ...\n```\n\nHmm. Let me look at hmac.hjson KEY_0:\n\n```\n    { name: \"KEY_0\",\n      desc: \"Secret key\",\n      swaccess: \"wo\",\n      hwaccess: \"hrw\",\n      hwqe:     \"true\",\n      regwen:   \"KEY_REGWEN\",\n      fields: [...]\n    },\n```\n\nWith `regwen: \"KEY_REGWEN\"`, regtool generates in reg_top:\n\n```systemverilog\n  assign key_0_we = addr_hit[9] && reg_we && !reg_error && key_regwen_qs;\n```\n\nYes — I'm now confident: the regtool-generated reg_top gates `_we` with the regwen register's qs. This is the standard pattern (e.g., in flash_ctrl, keymgr reg_tops). So `key_0_we` includes `key_regwen_qs`.\n\nSo the invariant: when KEY_REGWEN.EN=0, writing KEY_0 must not propagate: `reg2hw.key[0].qe` stays 0 and secret_key unchanged.\n\nSignal: `u_dut.reg2hw.key[0].qe` — check it never pulses while regwen=0. Or check `u_dut.secret_key` unchanged. I'll use signal `u_dut.reg2hw.key[0].qe` with rule cfg_block_gating. Hmm, but the more robust check is on secret_key value. The rule cfg_block_gating: \"cfg_block=1 时敏感写必须被拒绝\". The check: write attempt while blocked → no effect. Signal could be `u_dut.secret_key` (value unchanged). I'll use secret_key for value-based check. Hmm, but then two invariants on same signal... that's fine.\n\nActually, let me use for the gating check the qe signal since it directly shows the write was rejected at the interface: signal `u_dut.reg2hw.key[0].qe`. And rationale mentions KEY_REGWEN.\n\n**Invariant 5: cfg_regwen_gates_cfg_writes** — CFG_REGWEN. In hmac.hjson:\n\n```\n    { name: \"CFG_REGWEN\",\n      desc: \"Configuration write enable. ... cleared when HMAC operation is in progress\",\n      swaccess: \"ro\",\n      hwaccess: \"hwo\",\n      ...\n    },\n    { name: \"CFG\",\n      ...\n      regwen: \"CFG_REGWEN\",\n      ...\n    }\n```\n\nYes, CFG has regwen CFG_REGWEN, and HW clears CFG_REGWEN when not idle (when hash started). So invariant: while HMAC operation in progress (CFG_REGWEN=0), writes to CFG (e.g., toggling sha_en or endian_swap) must be rejected.\n\nSignal: `u_dut.reg2hw.cfg.hmac_en.qe` or `u_dut.reg2hw.cfg.sha_en.qe`. Check: no qe pulse while CFG_REGWEN=0.\n\nTrigger: write CFG (hmac_en=1, sha_en=1), write CMD hash_start (0x1), then attempt write CFG sha_en=0 → must be rejected (CFG_REGWEN now 0).\n\nHmm, CMD register: `CMD.HASH_START = 0x1`, `CMD.HASH_PROCESS = 0x2`. Writing CMD.HASH_START=1 starts.\n\n**Invariant 6: digest_core_reg_consistent**\n- rule: reg_core_consistent\n- After hash_process completes (digest_valid=1), DIGEST_0..15 readback must equal core digest `u_dut.u_hmac_core.u_sha2.digest[...]`.\n- signal: `u_dut.u_hmac_core.u_sha2.digest` vs `u_dut.hw2reg.digest[0].d` — the invariant: hw2reg.digest[i].d == sha2 digest[i] when digest_valid.\n- signal: `u_dut.hw2reg.digest[0].d` — hmm, the consistency check compares two signals. The format has one \"signal\" field. I'll name the primary signal as the core digest and describe consistency in rationale: \"DIGEST_0..15 (hw2reg) 必须等于 u_sha2.digest\". Or signal: `u_dut.u_hmac_core.u_sha2.digest[0]`. \n\nHmm, in sha2.sv, the digest register: `logic [31:0] digest[NUM_WORDS];` where NUM_WORDS=16 (SHA-2 512 → 16 words of 32 bits). And hmac.sv:\n\n```systemverilog\n  for (genvar i = 0; i < NumDigestRegs; i++) begin : gen_digest_regs\n    assign hw2reg.digest[i].d = digest_sw[i]; // byte-swapped if digest_swap\n  end\n```\n\nSomething like that. There's digest_swap handling. The consistency: after done, DIGEST regs == core digest (modulo digest_swap byte order). I'll phrase: with digest_swap=0, DIGEST_i must equal u_sha2.digest[i].\n\n**Invariant 7: digest_read_requires_idle** (access_control) / err_code_coherent\n- Reading DIGEST while not idle → error sw_read_digest_without_idle, ERR_CODE set.\n- rule: err_code_coherent\n- signal: `u_dut.hw2reg.err_code.sw_read_digest_without_idle.d` or `u_dut.err_code`? In hmac.sv:\n\n```systemverilog\n  // Error code\n  typedef enum logic [31:0] {\n    SwErrNone                  = 32'h0000_0000,\n    SwInvalidKeyLength         = 32'h0000_0001,\n    SwReadDigestWithoutIdle    = 32'h0000_0002,\n    SwReadMessageWithoutIdle   = 32'h0000_0003,\n    SwShaDisabled              = 32'h0000_0004\n  } err_code_e;\n```\n\nAnd `hw2reg.err_code.d = err_code; hw2reg.err_code.de = err_valid;`\n\nThe ERR_CODE register: `ERR_CODE` with field ERR_CODE [31:0], swaccess ro. So after illegal digest read during busy, ERR_CODE must equal 0x2 (sw_read_digest_without_idle).\n\nsignal: `u_dut.hw2reg.err_code.d` — check value == SwReadDigestWithoutIdle after trigger.\n\ntrigger_regs: [{\"reg\": \"CFG\", \"data\": \"0x3\"}, {\"reg\": \"CMD\", \"data\": \"0x1\"}, {\"reg\": \"DIGEST_0\", \"data\": \"0x0\"}] — reading DIGEST_0 while running. But wait — the actions are write/step/sig_read; a \"read\" of DIGEST_0 isn't a write... The trigger is a register write sequence. Hmm, the error is triggered by a *read* of digest while busy. The trigger_regs only support writes. Hmm.\n\nCan I trigger the error with writes only? sw_sha_disabled: write CFG with sha_en=0, hmac_en=1, then write CMD.HASH_START=1 → error sw_sha_disabled (ERR_CODE=0x4). That's write-triggered! \n\nAlso sw_invalid_key_length: hmm, key length — in current OpenTitan HMAC, is there a key length config? I don't think so anymore (KEY_LEN was removed). The ERR_CODE has sw_invalid_key_length in some versions. Current hmac.hjson ERR_CODE:\n\n```\n    { name: \"ERR_CODE\",\n      ...\n      fields: [\n        { bits: \"31:0\", name: \"ERR_CODE\", ... }\n      ]\n    }\n```\n\nHmm, actually ERR_CODE in hmac.hjson:\n\n```\n    { name: \"ERR_CODE\",\n      desc: '''\n        HMAC error code.\n        ...\n      '''\n      swaccess: \"ro\",\n      hwaccess: \"hwo\",\n      fields: [\n        { bits: \"31:0\" }\n      ]\n    },\n```\n\nThe error codes are documented in the header: sw_invalid_key_length=1, sw_read_digest_without_idle=2, sw_read_message_without_idle=3, sw_sha_disabled=4.\n\nFor write-only triggers, sw_sha_disabled works: CFG with sha_en=0 then CMD.HASH_START → ERR_CODE=0x4.\n\nSo invariant: err_code_coherent — after starting hash with sha_en=0, ERR_CODE must be set to SwShaDisabled (0x4).\n\nsignal: `u_dut.hw2reg.err_code.d` (or `u_dut.err_code`).\n\ntrigger: [{\"reg\": \"CFG\", \"data\": \"0x1\"}, {\"reg\": \"CMD\", \"data\": \"0x1\"}] — CFG=0x1 means hmac_en=1, sha_en=0. Then CMD.HASH_START=1. Check ERR_CODE == 0x4.\n\nHmm wait, does hash_start with sha_en=0 trigger the error? In hmac.sv:\n\n```systemverilog\n  // Error\n  assign sw_sha_disabled = hash_start & ~reg2hw.cfg.sha_en.q;\n  ...\n  assign err_code = sw_sha_disabled ? SwShaDisabled : ...;\n```\n\nYes, I believe hash_start while sha_en=0 sets err_code to SwShaDisabled. \n\n**Invariant 8: fsm_sparse_encoding**\n- signal: `u_dut.u_hmac_core.hmac_state` (or u_state_regs.state_o)\n- rule: fsm_sparse_encoding\n- trigger: [{\"reg\": \"CFG\", \"data\": \"0x3\"}, {\"reg\": \"CMD\", \"data\": \"0x1\"}, {\"reg\": \"CMD\", \"data\": \"0x2\"}] — run a hash and check state stays in legal encodings throughout.\n- rationale: FSM.SPARSE — state must always be one of the one-hot/sparse encodings; any other value indicates fault injection.\n\nWhat are the actual legal encodings? Let me try to recall hmac_core.sv from OpenTitan master:\n\n```systemverilog\n  // FSM states\n  typedef enum logic [2:0] {\n    HMAC_IDLE = 3'b001,\n    HMAC_MSG  = 3'b010,\n    HMAC_IPAD = 3'b100,\n    HMAC_OPAD = 3'b... \n```\n\nHmm. Let me think about the actual hmac_core.sv code:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    Idle,\n    Init,\n    IPad,\n    Msg,\n    OPad\n  } hmac_state_e;\n```\n\nI've definitely seen `hmac_state_e` with Idle, Init, IPad, Msg, OPad... Hmm wait, actually I think it's:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    HMAC_IDLE = 3'b001,\n    HMAC_INIT = 3'b010,\n    HMAC_IPAD = 3'b100,\n    HMAC_MSG  = 3'b... \n```\n\n5 states, 3 bits. With sparse encoding via prim_sparse_fsm_flop, the enum values would be defined sparsely. Let me just say legal set = {HMAC_IDLE, HMAC_INIT, HMAC_IPAD, HMAC_MSG, HMAC_OPAD} encodings per hmac_state_e. In the JSON, I'll write the signal and note the legal set in rationale. The checker presumably validates against the enum.\n\nActually, I found it — from OpenTitan hmac_core.sv (recent master):\n\n```systemverilog\n  // SEC_CM: FSM.SPARSE\n  typedef enum logic [2:0] {\n    HMAC_IDLE    = 3'b001,\n    HMAC_MSG     = 3'b010,\n    HMAC_IPAD    = 3'b100,\n    HMAC_OPAD    = 3'b... \n```\n\nHmm, I really don't remember. Let me think about the flow: idle → (hash_start & hmac_en) → init (compute ipad into shaf) → msg → opad → idle. States: Idle, Init, IPad?, Msg, OPad. Hmm, actually the ipad computation happens in \"Init\" state feeding the shaf. Let me look at it differently:\n\nFrom actual code (I'm recalling the DV testbench): hmac_core FSM states in DV: `HmacIdle, HmacMsg, HmacIPad, HmacOPad`? Hmm.\n\nOK here's what I remember more concretely from hmac_core.sv:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    Idle,\n    Init,\n    IPad,\n    Msg,\n    OPad\n  } hmac_state_e;\n\n  hmac_state_e hmac_state, hmac_state_d;\n\n  always_comb begin\n    hmac_state_d = hmac_state;\n    hash_done_o  = 1'b0;\n    hash_start_ready_o = 1'b0;\n    ...\n    unique case (hmac_state)\n      Idle: begin\n        if (hash_start_i && hmac_en_i) begin\n          hmac_state_d = Init;\n        end\n      end\n      Init: begin\n        // feed ipad to shaf\n        if (shaf_rvalid && shaf_rready) begin // or when 64B fed\n          hmac_state_d = Msg;\n        end\n      end\n      ...\n```\n\nHmm, wait — is there an IPad state separate from Init? I think: Idle → Init (process ipad block) → Msg → OPad (process opad block) → Idle. That's 4 states. Plus maybe \"IPad\"/\"OPad\" for the padding feeding. \n\nActually, from the real OpenTitan hmac_core.sv:\n\n```systemverilog\n  typedef enum logic [2:0] {\n    HMAC_IDLE = 3'b001,\n    HMAC_MSG  = 3'b010,\n    HMAC_IPAD = 3'b100,\n    HMAC_OPAD = 3'b... \n```\n\nI give up trying to recall exact encodings; it doesn't matter for the JSON. I'll write: legal encodings are the sparse one-hot codes of hmac_state_e (Idle/Init/IPad/Msg/OPad). Actually, to be safe, I'll phrase rationale generically: \"hmac_state 必须始终处于 hmac_state_e 定义的 sparse 编码集合内\".\n\nHmm, wait. Let me reconsider: does hmac_core.sv actually have SEC_CM: FSM.SPARSE? Let me think... The hmac.hjson SEC_CM comments:\n\n```\n  // SEC_CM: BUS.INTEGRITY\n```\n\nand in hmac_core.sv / sha2.sv... I recall sha2.sv has a FSM too. Recent OpenTitan added FSM.SPARSE to many IPs. For HMAC... Let me think about whether hmac has sparse FSM. Hmm. I believe recent hmac_core.sv does have:\n\n```systemverilog\n  // SEC_CM: FSM.SPARSE\n```\n\nHmm, not sure. But the task explicitly lists fsm_sparse_encoding as one of the 12 rules and asks for invariants per data path including 状态机. HMAC has FSMs (hmac_state, sha2 state). So proposing fsm_sparse_encoding is appropriate regardless.\n\n**Invariant 9: bus_intg_check**\n- signal: `u_dut.u_reg.intg_err` — hmm. Let me think about what's accessible. In hmac_reg_top.sv, there's `intg_err` signal? The generated reg_top for IPs with alerts:\n\n```systemverilog\n  logic intg_err;\n  ...\n  tlul_adapter_reg #(\n    ...\n  ) u_reg_if (\n    ...\n    .intg_err_o (intg_err)\n  );\n```\n\nHmm, but hmac_reg_top given in the problem — does it have intg? The problem shows lines 219-220, 874-891, 1953, 2128-2129, 2311. Not enough to tell. Modern OpenTitan reg_top does include intg_err and fatal alert logic. I'll use `u_dut.u_reg.intg_err`.\n\nHmm, wait — actually, does HMAC have a fatal alert? Let me carefully recall hmac.hjson from OpenTitan master (2023-2024):\n\n```\n{ name: \"hmac\",\n  ...\n  clocking: [...],\n  bus_interfaces: [...],\n  alert_list: [\n    { name: \"fatal_fault\",\n      desc: '''This fatal alert is triggered when a fatal TL-UL bus integrity fault is detected inside the HMAC unit.\n            '''\n    }\n  ],\n  ...\n  // SEC_CM: [\"BUS.INTEGRITY\"]\n```\n\nI'm fairly confident yes — HMAC has fatal_fault alert tied to bus integrity. OK.\n\nThe invariant: when a TL-UL write with corrupted integrity hits the HMAC, `intg_err` must assert (and latch, raising fatal_fault alert). Signal: `u_dut.u_reg.intg_err`. Trigger: a corrupted bus write — but trigger_regs are register writes... A corrupted write isn't a normal register write. Hmm. The trigger could be modeled as write to any register with bad intg — the checker framework presumably can inject. I'll specify trigger_regs as [{\"reg\": \"KEY_0\", \"data\": \"0xA5A5A5A5\"}] with note that the write is issued with corrupted TL-UL integrity bits (checker injects). Rationale: BUS.INTEGRITY SEC_CM — intg error must be detected and raised as fatal alert / d_error.\n\nAlternatively, signal `u_dut.u_reg.u_reg_if.intg_err_o`. I'll go with `u_dut.u_reg.intg_err`.\n\n**Invariant 10: interrupt_first_event**\n- signal: `u_dut.intr_hmac_done_o` or `u_dut.hw2reg.intr_state.hmac_done.de`\n- rule: interrupt_first_event\n- trigger: full hash run: CFG=0x3, KEY writes, CMD=0x1 (start), MSG_FIFO writes, CMD=0x2 (process) → wait done. Check intr_hmac_done pulses exactly once per completed hash; doesn't re-assert until SW clears intr_state.\n- rationale: hmac_done interrupt must fire only on the first completion event; no repeated assertion while pending.\n\n**Invariant 11: wipe_clears_digest?** — Hmm, does WIPE_SECRET clear the digest? I don't think so. The digest is cleared on... reset? Or when new hash starts. Not by wipe_secret. Skip.\n\n**Invariant 12: status_digest_valid gating digest reads** — access_control: DIGEST registers readable only when STATUS.digest_valid=1... Actually the HW doesn't block the read; it flags an error if read while not idle. The \"access control\" here is the error mechanism. I already have err_code_coherent for sha_disabled. Could add one for digest read while busy — but trigger needs a read action. The actions available: write, step, sig_read. sig_read is white-box signal read, not bus read. Hmm, but maybe sig_read can be used to check the error logic: after sig_read of digest path... no.\n\nHmm, wait — maybe I can frame the digest-read error differently: the check is that `err_code` updates only per the error conditions. Let me instead do: \"digest_read_blocked_when_busy\": signal `u_dut.hw2reg.err_code.d`, rule err_code_coherent, trigger includes a digest read... can't.\n\nAlternatively, use sig_read to check internal gating: e.g., `u_dut.digest_sw` readable... no.\n\nLet me reconsider: maybe the framework's \"sig_read\" can also observe that reading digest while busy sets err_code — the trigger_regs would be the write sequence to get into busy state, and then the checker performs a bus read? Not per spec. I'll keep triggers write-based.\n\nActually — hmm, one more thought: maybe I can include the digest-read error invariant with trigger_regs being the write sequence to reach busy state, and the \"check\" being that ERR_CODE remains 0 until an illegal read occurs... that's weak. Better to skip and keep the sha_disabled err_code invariant.\n\n**Invariant: reg_core_consistent for key**\n- After writing KEY_0..KEY_31, `u_dut.secret_key` must equal the concatenation of written values.\n- signal: `u_dut.secret_key`\n- trigger: [{\"reg\": \"KEY_0\", \"data\": \"0x03020100\"}, ..., {\"reg\": \"KEY_31\", \"data\": \"0x1F1E1D1C\"}]\n- check: secret_key == {KEY_31..KEY_0 written values}.\n\nNote: key word order — `secret_key_i // {word0, word1, ..., word7}` hmm, 1024 bits with 32 words KEY_0..KEY_31. In hmac.sv:\n\n```systemverilog\n  for (genvar i = 0; i < 32; i++) begin : gen_key_regs\n    assign reg2hw.key[i].qe = key_we & (key_addr == i); // no...\n  end\n```\n\nActually reg2hw.key is an array of 32 structs {q, qe}. In hmac.sv:\n\n```systemverilog\n  logic [1023:0] secret_key;\n  always_ff @(posedge clk_i or negedge rst_ni) begin\n    if (!rst_ni) begin\n      secret_key <= '0;\n    end else if (reg2hw.wipe_secret.qe) begin\n      secret_key <= '0;\n    end else begin\n      for (int i = 0; i < 32; i++) begin\n        if (reg2hw.key[i].qe) begin\n          secret_key[32*i +: 32] <= reg2hw.key[i].q;\n        end\n      end\n    end\n  end\n```\n\nHmm wait, is the key storage in hmac.sv or hmac_core.sv? The given snippet shows hmac_core.sv line 11: `input [1023:0] secret_key_i` — so hmac_core receives the key as input; storage is in hmac.sv. And lines 139-155 in hmac_core.sv compute i_pad/o_pad from secret_key_i. So `u_dut.secret_key` (in hmac.sv) is the storage. \n\nLet me double check hmac.sv structure: \n\n```systemverilog\nmodule hmac (\n  input clk_i, rst_ni,\n  // Bus interface\n  input tlul_pkg::tl_h2d_t tl_i,\n  output tlul_pkg::tl_d2h_t tl_o,\n  // Alerts\n  input prim_alert_pkg::alert_rx_t alert_rx_i,\n  output prim_alert_pkg::alert_tx_t alert_tx_o,\n  // Interrupts\n  output logic intr_hmac_done_o,\n  output logic intr_fifo_empty_o,\n  output logic intr_hmac_err_o,\n  ...\n);\n\n  import hmac_reg_pkg::*;\n  hmac_reg2hw_t reg2hw;\n  hmac_hw2reg_t hw2reg;\n  hmac_reg_top u_reg (...);\n\n  logic [1023:0] secret_key;\n  ...\n  assign secret_key_i = secret_key; // hmm, or wire\n  hmac_core u_hmac_core (\n    .secret_key_i (secret_key),\n    ...\n  );\n```\n\nYes. So `u_dut.secret_key` is right for the key storage. And wipe: `reg2hw.wipe_secret.qe` clears it.\n\nWait, actually — I want to double-check the wipe behavior in hmac.sv. From actual OpenTitan hmac.sv:\n\n```systemverilog\n  // SEC_CM: KEY.SEC_WIPE\n  always_ff @(posedge clk_i or negedge rst_ni) begin\n    if (!rst_ni) begin\n      secret_key <= '0;\n    end else if (reg2hw.wipe_secret.qe) begin\n      secret_key <= '0;\n    end else begin\n      for (int i = 0 ; i < 32 ; i++) begin\n        if (reg2hw.key[i].qe) begin\n          secret_key[32*i +: 32] <= reg2hw.key[i].q;\n        end\n      end\n    end\n  end\n```\n\nYes, I'm confident this is the actual code (SEC_CM: KEY.SEC_WIPE). \n\nAlso the msg FIFO wipe:\n\n```systemverilog\n  prim_fifo_sync #(\n    .Width(FifoDWidth),\n    .Pass(0),\n    .Depth(MsgFifoDepth)\n  ) u_msg_fifo (\n    .clk_i,\n    .rst_ni,\n    .clr_i   (reg2hw.wipe_secret.qe),\n    ...\n  );\n```\n\nYes.\n\nAlso, hmac_core wipes... hmm, there's also wiping of the SHA-2 internal state? In sha2.sv, when a new hash starts, the digest is cleared? There's `sha2_hash_done` → digest wiped after read? Hmm, there's a feature: digest registers are wiped when... Let me not overreach.\n\nNow, also consider: **STATUS.hmac_running / idle consistency** — could be a state invariant but not one of the 12 rules directly. Skip.\n\n**monotonic_counter**: HMAC has message length counter (`msg_count`), which counts bytes fed. It only increases during a message. Not really a security counter. Could propose: msg_count only increases while hashing. Rule monotonic_counter. Signal: `u_dut.msg_count` or `u_dut.u_hmac_core.msg_count`. Hmm — is this security-relevant? The message length counter affects the padding/length encoding; a counter that could decrease would corrupt the hash. It's a data-integrity property. I could include it: \"msg_count_monotonic\": during an operation, msg_count (bytes processed) must never decrease. Trigger: CFG, CMD start, MSG writes, CMD process. Check across steps: msg_count non-decreasing.\n\nHmm, is msg_count in hmac.sv? Yes: `logic [63:0] msg_count;` in hmac.sv, counts message bytes written to FIFO. It resets on hash_start. So invariant: between hash_start and hash_process completion, msg_count is non-decreasing. That fits monotonic_counter (\"只增不减（除非显式清除）\" — explicit clear = hash_start reset). I'll include it.\n\n**changes_across_runs**: no randomness in HMAC. Skip — forcing it would be wrong. The task says \"针对每个安全敏感数据路径（密钥/种子/掩码/摘要/擦除/状态机/总线/中断）\" — HMAC has no seed/mask paths, so skip those. Good.\n\n**debug_lock_enforce**: HMAC has no DFT/debug lock signals. Skip.\n\n**access_control**: I have cfg_block_gating for KEY_REGWEN and CFG_REGWEN. Could also add a generic access_control: digest readable only when digest_valid — but again read-triggered. Hmm.\n\nActually wait — maybe I can do access_control on digest via the error path: the HW blocks... no, HW doesn't block; it errors. The err_code_coherent invariant covers it if triggered by read. Since triggers are writes, I'll do the sha_disabled one.\n\nHmm, but actually, let me reconsider whether sig_read could serve as the \"read\" action for triggering digest-read errors. The spec: \"不变量必须能用这些动作验证：write(寄存器)、step(时钟)、sig_read(白盒信号)。\" — sig_read reads white-box signals, it's for checking, not for stimulating bus reads. So no.\n\nLet me also think about **wipe_clears for digest on reset**: after reset, DIGEST_0..15 must read 0. Rule wipe_clears. Signal: `u_dut.hw2reg.digest[0].d` or the digest storage. In hmac.sv, digest regs: hw2reg.digest[i].d driven from... let me recall:\n\n```systemverilog\n  // Digest registers\n  logic [31:0] digest_sw[NumDigestRegs];\n  ...\n  for (genvar i = 0; i < NumDigestRegs; i++) begin : gen_digest\n    assign hw2reg.digest[i].d = digest_swap ? conv_endian(digest[i], 1) : digest[i];\n  end\n```\n\nHmm, and digest comes from hmac_core/sha2. On reset, sha2 digest is 0. So after reset, DIGEST reads 0. This is a wipe_clears invariant: \"digest_reset_zero\". Trigger: none (reset) — but trigger_regs required? The format shows trigger_regs as a list; maybe can be empty for reset-triggered."
}