# reports/ 目录结构

按**跑漏洞的日期**归档，文件夹名 = `YYYYMMDD`（如 `20260903/`）。

- `YYYYMMDD/`：当天产出的漏洞报告、扫描汇总。每次跑测的产物放当天的日期目录。
- `CTF-SUMMARY-REPORT.md`：累计汇总报告（活文档，持续追加章节），始终留在本目录根部。
- 归档以文件修改时间为准；`fuzz/` 下的机器可读结果（JSON）不搬，保持脚本输出路径稳定。
